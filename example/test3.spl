program test;
var
	x : integer;
    y : integer;
    z : integer;
    b : boolean;
    c : char;


function findMin(x, y: integer): integer;
begin
    findMin := 3 + 10 + x;
end
;


begin
    c := 'a';
    write("%c", c);
    x := 20 / 5;
    y := x - 2;
    z := y + x;
    b := ( x < y ) or ( true );
    if x <= y  then
        x := 10
    else
        x := 15;
    x := findMin(x, y);
    write("%d %d %d", x, y, z);
	while x < 30 do begin
	    x := x + 1;
	    write("%d", x);
	end;
end
.
